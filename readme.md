## docker-debian-es
```bash
$ docker build -t srph/debian-es
```
Dockerfile based on `debian:jessie` with `build-essential` and `curl`.

## Example Dockerfile
```Dockerfile
FROM srph/debian-es

# ... your configuration
```